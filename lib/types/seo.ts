/**
 * Type for the SEO object including the ogImage field.
 * Used as a type assertion until TinaCMS regenerates types.
 */
export interface SeoField {
  title?: string | null;
  description?: string | null;
  ogImage?: string | null;
}
